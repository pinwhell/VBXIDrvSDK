#pragma once

#include <cstdarg>

// REGPARAMDECL(void *) memset(void * s, int c, size_t count);
// REGPARAMDECL(char*) strstr(const char* haystack, const char* needle);
// REGPARAMDECL(char*) strnstr(const char* haystack, const char* needle, unsigned int size);
// REGPARAMDECL(char*) memcpy(void* to, const void* from, unsigned int len);
// REGPARAMDECL(char*) strcpy(char* destination, const char* source);
// REGPARAMDECL(char*) strncpy(char* destination, const char* source, int len);
// REGPARAMDECL(unsigned int) strlen(const char* str);
// REGPARAMDECL(int) strncmp(const char* str1, const char* str2, unsigned int len);
// REGPARAMDECL(int) strcmp(const char* str1, const char* str2);
// const char * my_strrchr(const char * str, int character);
// REGPARAMDECL(int) vsnprintf(char *str, size_t size, const char *format, va_list ap);